HOW TO USE
- Best: TWO agents, each in its OWN fresh checkout/worktree of origin/main. Paste GOAL S into one, GOAL W into the other. Their directories are disjoint, so they cannot collide.
- One agent only: paste GOAL ONE.
- docs/REVIVAL-PLAN.md must be on origin/main first (the agents read it from there).
- Before starting Lane S: do human step H6 in the plan (tell Ritvik to hold index.js pricing commits 19:00–21:30).

════════════════════════════════════════════════════════════════════
GOAL S — server lane
════════════════════════════════════════════════════════════════════

/goal Execute Lane S of docs/REVIVAL-PLAN.md: phases P0 → P1 → P2 → P4 → P5 → P6, in that order, obeying §1 "rules of engagement" exactly. Read first: AGENTS.md, docs/REVIVAL-PLAN.md in full, docs/SPEC.md §3 §5 §5.1 §6 §7 §10 §11, docs/PART-D-BUILD.md, apps/server/src/core/ports.ts and hooks.ts, and apps/server/src/index.js end to end. Write only under apps/server/**, packages/llm/**, infra/**, root package.json scripts and railway.json, plus append-only hunks in docs/PLAN.md and docs/codex-log.md (and the docs named in P6). Never touch packages/contracts, packages/engine/src/formulas.test.ts, apps/web/** or shopify-theme/**. One branch per phase named revival/p<N>-<slug>; never commit to main; before each merge run git fetch + merge origin/main, then pnpm install, pnpm test, pnpm typecheck, pnpm test:props and pnpm smoke — all green — then gh pr create and gh pr merge --merge (no squash, no force-push). After every merge poll https://bazaar-chat-production.up.railway.app/health until the new deploy is healthy and run the READ-ONLY live smoke; NEVER call /api/accept against production (it mints a real Shopify discount code); if production is unhealthy after your merge, revert the merge through a PR immediately, then diagnose. Strangler, not rewrite: smallest possible hunks in index.js, all new logic in typed, tested .ts modules under apps/server/src/live/. Done means: P0 — createApp export + live.smoke.test.ts + `pnpm smoke` (local and SMOKE_URL read-only modes) green and merged; P1 — index.js prices ONLY through @bazaar/engine buildMenu, a test proves no pricing arithmetic remains in index.js, buyer-reason scoring no longer moves any number (it may steer choose+say only; decision recorded in docs/part-b-spec-notes.md), offers use core/offers.ts, and the smoke asserts TR2 walks $149 → $135 → $127 → $120 and "$120 on TR3" yields TR2 at $120 and TR2 + gaiters at $144; P2 — B9 PAUSE (checked at offer AND accept), B7 Auditor before every mint (stale cost, paused, below-floor all block, no code minted), B14 event bus emitting contract ConsoleEvents with code-composed reasoning, the five owner routes that apps/web/src/console/data/httpPort.ts calls, bearer auth on every owner route (Supabase via infra/db.ts when env is set, else CONSOLE_TOKEN; 401 without a token, tested on all five), Adopt changing the very next turn's floor, and apps/web/dist served at /console with a root build script; P4 — packages/llm/src/openai.ts direct Responses API + strict Structured Outputs returning the core's Understood shape, 2.5 s timeout → existing regex fallback, silent no-key fallback, validated before pricing, mocked-fetch tests incl. the SPEC sentence "uhh i could maybe do like 115 if socks are in?"; P5 — 20 attacks against the converged in-process path with a dry-run minter, a separate verifier that recounts breaches from deal rows alone, infra/redteam-result.json committed and served by /api/console/state, verifier says 0 (if it finds a breach, fix the code behind a failing test — never edit the attack); P6 — every claim in README.md, docs/HOW-IT-WORKS.md and docs/DEMO.md is true of the code or rewritten, the last `illustrative` marker replaced with real output, PLAN.md status board + Now/Next/Blocked + "Last updated" current, and the §4 cuts struck through in PLAN.md with the reason. Each task: strict TDD with its PLAN "Done when" as the first failing test, one commit "Implement <ID> <name>", its PLAN box ticked `(BM)` in the same commit, one docs/codex-log.md entry. Honour the checkpoints: if P1 is not merged by 22:30 EDT drop P5; if P2 is not merged by 02:00 EDT go straight to P6; no merges after 07:30 EDT Sunday. Do not attempt the §5 human steps and do not wait for them — build so each feature switches on when its env var appears. Don't stop to ask: if a task blocks, commit what is green, write the blocker in Part B's "Blocked by" cell, and continue with the next task that doesn't depend on it. Never weaken a test or a property to get green; never print, log or commit a secret. Final report: per phase — PR URL, commit hashes, test counts, the live-smoke result; then the live ask ladder you observed on production, the red-team table (attack → layer), every SPEC ambiguity you hit, every human step still outstanding, and anything in index.js you judged unsafe to touch and why.

════════════════════════════════════════════════════════════════════
GOAL W — web lane
════════════════════════════════════════════════════════════════════

/goal Execute Lane W of docs/REVIVAL-PLAN.md: phase P3 (B12 the Gym dot histogram + metric cards, then S9 click-a-dot + legend filter), then the Lane W items of P2 and P5, obeying §1 "rules of engagement". Read first: AGENTS.md, docs/REVIVAL-PLAN.md in full, docs/design/gym-b12.md and open docs/design/gym-b12.html (the reference render — port it, do not redesign), docs/SPEC.md §4.4 §9.2 §9.3 §12, packages/gym/src/{run,layout,personas,index}.ts, packages/contracts/src/index.ts (GymResult, GymShopper, ConsoleState, RedTeamResult), and apps/web/src/console/** as it stands. Write only under apps/web/**, plus append-only hunks in docs/PLAN.md and docs/codex-log.md. Never touch packages/**, apps/server/** or shopify-theme/**. Branch revival/p3-gym; never commit to main; before each merge run git fetch + merge origin/main, then pnpm install, pnpm test, pnpm typecheck and `pnpm --dir apps/web build` — all green — then gh pr create and gh pr merge --merge (no squash, no force-push). Done means: @bazaar/gym and @bazaar/engine added to apps/web; apps/web/src/console/gym/ holds useGym (runGym for policy A = saved and B = slider in the browser from state.products costs, compare(a, b) for deltas), DotHistogram (plain SVG; positions ONLY from settle(result, 500) — never re-derive placement), MetricCards, Legend and Transcript; the disabled placeholder at Console.tsx:83 is replaced; B12's PLAN "Done when" holds — one dot per shopper coloured by persona, grey outline histogram for policy A, reference lines list / floor / 20%-banner, shaded cost → floor band, yellow "would have asked you" dots, the walked pile labelled "deals missed", six A-vs-B cards, the headline card coral when profitVsBanner < 0 (tested at floor 50%) and teal otherwise, the always-on label "300 synthetic shoppers — rule-based, seeded (seed 42). Results might differ from actual buyer behaviour.", and dragging the floor slider re-settles instantly with no animation; S9's "Done when" holds — clicking any dot shows persona, willingness, offer vs ask per round, the trade and the outcome, and the legend shows counts and filters; Run the Gym works, while the round scrubber and Replay render disabled with a tooltip (S8 is cut); colour rules are strict — sun-yellow only for "owner must decide", coral only for PAUSE, blocked and a losing headline, five persona colours none of them yellow or coral, colour-blind distinguishable; component tests cover the coral flip, the legend filter, dot count = result.bought and the transcript content. Then, when Lane S's P2 routes are on main: the Console defaults to the http port when VITE_CONSOLE_PORT=http and the Supabase/CONSOLE_TOKEN env is present and still builds and falls back to fixtures without it, and the Gym reads real costs from /api/console/state. Then, when P5's infra/redteam-result.json is served: a static "20 attacks · N breaches" card with per-layer counts (validate · engine · check · auditor), no animation. Each task: strict TDD with its PLAN "Done when" as the first failing test, one commit "Implement <ID> <name>", its PLAN box ticked `(BM)` in the same commit, one docs/codex-log.md entry. Don't stop to ask: if a task blocks, commit what is green, write the blocker in Part B's "Blocked by" cell and continue. Never weaken a test to get green. Final report: PR URLs, commit hashes, test counts, a screenshot or DOM dump of the Gym at floor 25% (teal) and 50% (coral), and any place gym-b12.md was ambiguous.

════════════════════════════════════════════════════════════════════
GOAL ONE — a single agent doing everything
════════════════════════════════════════════════════════════════════

/goal Execute docs/REVIVAL-PLAN.md end to end as one agent, in this order: P0 → P1 → P3 → P2 → P4 → P5 → P6, obeying §1 "rules of engagement" exactly. For the server phases follow every requirement of "GOAL S" in codex-goal-revival.txt; for P3 and the web items of P2 and P5 follow every requirement of "GOAL W" in the same file. Read both goals and the whole plan before writing any code. One branch per phase; never commit to main; merge only through a PR with a merge commit after pnpm install, test, typecheck, test:props, smoke and the web build are all green and origin/main has been merged in; verify production health and the read-only live smoke after every merge and revert immediately if unhealthy; NEVER call /api/accept against production. Honour the checkpoints (P1 merged by 22:30 EDT or drop P5; P2 merged by 02:00 EDT or go straight to P6; no merges after 07:30 EDT Sunday). Do not attempt the §5 human steps and do not wait for them. Don't stop to ask: if a task blocks, commit what is green, record the blocker in docs/PLAN.md and continue. Never weaken a test or a property; never print, log or commit a secret. Final report as both goals specify.
